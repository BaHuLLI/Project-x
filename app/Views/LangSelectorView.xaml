<UserControl x:Class="ProjectXProDash.Views.LangSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <ToggleButton x:Name="LanguageButton"
                      Height="44"
                      Background="{StaticResource SurfaceBrush}"
                      BorderThickness="0"
                      IsChecked="{Binding IsLanguagePopupOpen, Mode=TwoWay}">
            <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="12">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock HorizontalAlignment="Center"
                                       Foreground="#FFFFFF"
                                       FontFamily="{DynamicResource TelemetryFontFamily}"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Text="{Binding CurrentLanguage}" />
                            <TextBlock HorizontalAlignment="Center"
                                       Foreground="{StaticResource MutedBrush}"
                                       FontFamily="{DynamicResource UiFontFamily}"
                                       FontSize="9"
                                       FontWeight="SemiBold"
                                       Text="LANG" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>

        <Popup IsOpen="{Binding IsLanguagePopupOpen}"
               Placement="Top"
               PlacementTarget="{Binding ElementName=LanguageButton}"
               AllowsTransparency="True"
               StaysOpen="False">
            <Border Margin="0,0,0,10"
                    Padding="10"
                    Background="#B31A1E24"
                    BorderThickness="0"
                    CornerRadius="12">
                <Border.Effect>
                    <BlurEffect Radius="18" />
                </Border.Effect>

                <StackPanel DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=Popup}}">
                    <ItemsControl ItemsSource="{Binding Languages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Padding="12,8"
                                        Margin="0,2,0,2"
                                        HorizontalContentAlignment="Left"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Command="{Binding DataContext.SelectLanguageCommand, RelativeSource={RelativeSource AncestorType=Popup}}"
                                        CommandParameter="{Binding}">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="RowSurface"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    CornerRadius="8">
                                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                  VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="RowSurface" Property="Background" Value="#33FF6A00" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <TextBlock Foreground="#FFFFFF"
                                               FontFamily="{DynamicResource UiFontFamily}"
                                               FontWeight="SemiBold"
                                               FontSize="12"
                                               Text="{Binding}" />
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>
